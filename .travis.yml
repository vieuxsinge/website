language: python
branches:
  only:
  - master
install:
- pip install pelican ghp-import
script:
- make publish
deploy:
  provider: script
  skip_cleanup: true
  script: ghp-import -p $TRAVIS_BUILD_DIR/output
  on:
    branch: master
